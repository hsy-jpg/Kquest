# K-Quest 관광 데이터 선별 로직

## 1. 목적과 결론

K-Quest는 관광 명소 카탈로그가 아니라 한국에 장기 체류하는 외국인이 동네를 반복해서 탐험하도록 돕는 앱이다. 따라서 API 데이터를 모두 적재하지 않고, 먼저 **Quest로 실행 가능한 데이터인지**를 검사한 뒤 **지역성(Local Score)**, 데이터 품질, 지역·유형 다양성을 함께 평가한다.

권장 선별 순서는 다음과 같다.

1. 하드 게이트로 위치 확인·안전·운영 가능성이 없는 항목을 제외한다.
2. `contenttypeid`, `lclsSystm1~3`, `cat1~3`, `title`, `overview`, 주소를 합쳐 의미를 분류한다.
3. Local Score(0~100)와 Data Quality Score(0~100)를 별도로 계산한다.
4. `Local Score >= 50`이고 `Data Quality Score >= 55`이며 분류 신뢰도와 안전 조건을 충족한 항목을 자동 채택한다.
5. 경계 항목은 검수 대기열로 보내고, 지역·유형별 quota와 중복 제거를 적용한 뒤 게시한다.

유명하다는 이유만으로 제외하지는 않는다. 유명 장소라도 주변 골목·시장·생활문화와 연결할 수 있으면 채택하되, 지역별 피드가 대표 명소로 잠식되지 않도록 별도 quota를 둔다.

## 2. 실제 API 표본에서 확인한 구조

2026-08-16 기준 `.env`의 `TOURAPI_SERVICE_KEY`로 한국관광공사 영문 관광정보 `EngService2`를 직접 조회했다. `areaBasedList2`에서 서울 716건, 부산 163건 등 지역별 목록을 확인하고 서울·부산·제주 각 6건, `searchFestival2`의 향후 행사 10건을 살펴봤다. 확인한 예시는 다음과 같다.

| 실제 항목 | contentType | 구 분류 | 신 분류 | 설계상 의미 |
|---|---:|---|---|---|
| Ahopsan Forest, 부산 | 76 | `A01/A0101/A01010400` | `NA/NA01/NA010200` | 숲·자연 탐방 후보 |
| 168 Stairs, 부산 | 76 | `A02/A0203/A02030400` | `EX/EX07/EX070200` | 동네 경관·골목 탐색 후보 |
| 40-step Culture & Tourism Theme Street, 부산 | 76 | `A02/A0203/A02030600` | `VE/VE04/VE040100` | 지역사·거리 탐색 후보 |
| ARKO Art Center, 서울 | 78 | `A02/A0206/A02060500` | `VE/VE07/VE070600` | 문화시설 후보 |
| 10 Corso Como Cheongdam, 서울 | 79 | `A04/A0401/A04010600` | `SH/SH05/SH050200` | 쇼핑이지만 지역성은 낮을 수 있음 |
| 105405 Magok, 서울 | 82 | `A05/A0502/A05020700` | `FD/FD02/FD020500` | 음식점 후보; 로컬 근거 추가 필요 |
| Andong Maskdance Festival | 85 | 행사 목록에서 `cat*` 비어 있음 | `EV/EV01/EV010100` | 전통 공연 축제 후보 |
| Chuncheon Makguksu & Dakgalbi Festival | 85 | 행사 목록에서 `cat*` 비어 있음 | `EV/EV01/EV010300` | 지역 음식 축제 후보 |

목록 응답에서 확인한 주요 필드는 `contentid`, `contenttypeid`, `title`, `addr1/2`, `areacode`, `sigungucode`, `lDongRegnCd`, `lDongSignguCd`, `mapx/y`, `firstimage/2`, `cat1/2/3`, `lclsSystm1/2/3`, `tel`, `createdtime`, `modifiedtime`다.

실제 응답상 주의점은 다음과 같다.

- `contentTypeId`는 큰 용도만 알려 준다. 같은 76 안에 자연과 문화·거리 장소가 함께 있으므로 단독 매핑하면 안 된다.
- 신 분류는 `NA`(자연), `VE`(관람·문화), `EX`(체험), `SH`(쇼핑), `FD`(음식), `EV`(행사), `AC`(숙박)처럼 의미가 잘 드러나 우선 활용할 가치가 있다.
- 행사 검색 표본은 `cat1~3`, `areacode`, `sigungucode`가 빈 경우가 있었지만 `lDongRegnCd`, `lDongSignguCd`, `lclsSystm*`, 주소와 좌표는 있었다. 지역 판정과 분류에 fallback이 필요하다.
- `arrange=O`의 첫 페이지는 이름순 성격이 강해 표본·수집 방식으로 부적합하다. 모든 페이지를 순회한 뒤 점수화해야 한다.
- 콘솔 표시에서 괄호 속 한글이 깨져 보일 수 있었다. 수집·저장·로그 전체를 UTF-8로 고정하고 원문을 임의로 정제하지 않는다.

## 3. 데이터 수집 범위

### 기본 포함 content type

| contentTypeId | 실제 의미 | 수집 정책 |
|---:|---|---|
| 76 | 관광지 | 우선 수집. 자연·역사·거리·체험을 하위 분류로 재판정 |
| 78 | 문화시설 | 우선 수집. 지역 박물관, 공방, 공연장, 작은 전시 공간 가점 |
| 79 | 쇼핑 | 조건부 수집. 전통시장·오일장·지역 상점가는 우선, 백화점·글로벌 브랜드는 감점 |
| 80 | 숙박 | 기본 Quest 대상에서는 제외. 한옥·템플스테이·농어촌 체험 숙박만 검수 후보 |
| 82 | 음식점 | 조건부 수집. 지역 음식·노포·시장 연계 근거가 있어야 채택 |
| 85 | 행사/축제 | 우선 수집하되 기간·상태 검증 필수 |
| 75 | 레포츠 | 조건부 수집. 걷기·자전거·해양·농촌 등 장소 기반 활동 우선 |

content type 목록은 운영 시 `categoryCode2` 결과로 동기화하고 ID를 코드에 의미 없이 하드코딩하지 않는다. 알 수 없는 신규 ID는 자동 게시하지 않고 검수 대상으로 둔다.

## 4. 1차 하드 게이트

다음 조건을 모두 통과해야 점수 계산으로 넘어간다.

### 필수 조건

- `contentid`, 영문 `title`이 존재한다.
- 유효한 `mapx`, `mapy`가 한국 범위 내에 있거나, 좌표가 없을 때 지오코딩 가능한 `addr1`이 있다.
- 장소형은 주소 또는 상세한 위치 설명이 있어 사용자가 도착 지점을 판단할 수 있다.
- 행사형은 `eventstartdate`, `eventenddate`를 확보하며 종료일이 수집 기준일 이전이 아니다.
- 금지·민감 장소, 사유지 무단 진입, 위험 행동을 요구할 가능성이 없어야 한다.
- 동일 `contentid`의 최신 `modifiedtime` 레코드만 유지한다.

### 자동 제외 또는 보류

- 일반 호텔·모텔, 대형 백화점, 글로벌 명품 매장, 프랜차이즈 지점처럼 로컬 탐험성이 거의 없는 항목
- 주소와 좌표가 모두 없거나 좌표가 명백히 비정상인 항목
- overview와 분류가 모두 부족해 무엇을 하는 장소인지 판단할 수 없는 항목
- 종료된 행사, 기간이 불명확한 일회성 행사, 행사 장소가 확정되지 않은 항목
- 차량 전용 전망 지점, 등반 난이도가 높은 장소 등 안전·접근 정보를 검증하지 못한 항목은 자동 게시 대신 검수
- 사용자가 구매·입장료 지불을 해야만 완료 가능한 Quest는 무료 대체 행동이 없으면 보류

이미지는 추천 품질에는 영향을 주지만 절대 필수로 두지 않는다. 이미지가 없는 훌륭한 지역 장소를 탈락시키지 않되, 자동 게시에는 자체 이미지 정책과 저작권 코드(`cpyrhtDivCd`) 검증을 추가한다.

## 5. 분류 해석 우선순위

필드 불일치와 결측을 견디기 위해 다음 순서로 판정한다.

1. `contenttypeid`: 장소/문화/쇼핑/숙박/음식/행사/레포츠의 큰 범위
2. `lclsSystm3` → `lclsSystm2` → `lclsSystm1`: 신 분류의 가장 구체적인 값 우선
3. `cat3` → `cat2` → `cat1`: 구 분류 fallback
4. `overview`, `title`, `addr1`: 지역 키워드와 실제 활동 단서
5. 분류와 텍스트가 충돌하면 구체적인 신 분류를 우선하되 자동 게시하지 않고 검수 표시

`title` 키워드만으로 확정하지 않는다. 예를 들어 이름에 “market”이 있어도 실제 분류가 행사(`EV`)면 Festival Quest가 기본이고, 시장 안에서 열리는 행사라는 보조 태그만 추가한다.

## 6. Local Score

### 점수식

`LocalScore = place_identity(0~35) + local_archetype(0~25) + participatory_value(0~20) + repeatability(0~10) + explicit_local_evidence(0~10) - penalties(0~30)`

각 항목은 규칙 기반 특징을 더하되 영역별 상한을 적용한다. 동일 근거를 여러 번 중복 가산하지 않는다.

Local Score는 장소의 지역성만 평가한다. `overview` 또는 상세 필드가 없다는 이유로 Local Score를 깎지 않는다. 결측은 Data Quality Score와 `reviewFlags`에서만 처리한다. 반대로 overview가 풍부하다는 이유만으로 지역성이 없는 장소에 Local 점수를 더하지 않는다.

### 6.1 Place Identity: 지역 고유성, 최대 35

- +20: `lcls`, `cat`, `title`, `addr1`, `contenttypeid` 중 둘 이상의 독립 신호가 지역 전통·역사·자연·생활문화를 가리킴
- +15: 전통·역사·무형유산·지역 공동체 관련 신/구 분류가 있고 주소로 지역이 확인됨
- +12: `NA*` 자연 분류와 시군구 주소가 함께 존재함
- +10: 지역명과 향토 음식·특산물·지역 산업이 title 또는 overview에 함께 나타남
- +5: 지역 예술가·주민·상인이 주체인 공간 또는 행사라는 근거가 있음

### 6.2 Local Archetype: 기본 지역성, 최대 25

overview가 없어도 구조화 필드와 제목으로 다음 유형이 확인되면 기본 가점을 부여한다.

- +25: 전통시장·정기시장·오일장 또는 지역 전통·특산물 축제
- +22: 골목·마을·계단·문화거리·생활사 공간
- +20: 지역 자연(`NA*` + 유효 지역/주소) 또는 전통·무형유산 행사
- +18: 지역 박물관·미술관·공방·주민 문화공간
- +15: 일반 지역 축제·공공예술 행사
- +12: 지역 음식점이며 지역명·향토음식·특산물 중 하나가 title/category에서 확인됨

한 항목에는 가장 높은 archetype 하나만 적용한다. 일반 음식점, 일반 쇼핑점, 컨벤션센터, 일반 숙박은 content type만으로 기본 가점을 받지 않는다.

### 6.3 Participatory Value: Quest 행동성, 최대 20

- +10: category 또는 title만으로도 관찰·찾기·걷기·체험 행동을 안전하게 구성 가능
- +6: 좌표가 있는 시장·거리·자연·문화시설·행사로 현장 인증이 가능
- +4: 구매·직원 상호작용 없이 무료 또는 저비용 미션을 구성 가능

### 6.4 Repeatability: 장기 체류 적합성, 최대 10

- +6: 상설 또는 정기 운영
- +4: 계절마다 다른 관찰·활동을 만들 수 있음
- +3: 주변 2~3개 장소와 도보 Quest 묶음 구성 가능

### 6.5 Explicit Local Evidence: 명시적 지역 근거, 최대 10

- +5: title에 시군구·동네·시장·마을·지역 자연명 등 장소 고유 명칭이 있음
- +5: `addr1`, 법정동 코드 또는 지역 코드로 구체 시군구를 확정할 수 있음

지역 분산성은 Local Score에서 제거한다. 비수도권이나 Quest가 부족한 시군구라는 사실은 콘텐츠 자체의 지역성이 아니므로 최종 정렬·quota 단계에서만 사용한다. 서울의 전통시장과 경북의 일반 체인점은 같은 콘텐츠 근거로 평가한다.

### 감점

- -25: 일반 체인·글로벌 브랜드·대형 복합 상업시설이며 지역 고유 체험 근거가 없음
- -20: 일반 숙박업소이며 지역 체험 근거 없음
- -10: category와 title을 함께 봐도 방문 외 행동을 만들기 어려움
- -10: 과잉 관광 위험 또는 주민 생활 침해 가능성이 큼
- -5: 입장·구매 강제가 사실상 필요한 장소

`overview` 결측, 짧은 설명, 유명 관광지라는 이유 자체는 감점하지 않는다. 유명 장소도 지역 자연·역사·생활문화 근거가 있으면 동일한 기본 가점을 받는다. 혼잡·과잉 관광 감점은 이름이나 주관적 인지도 대신 검증된 운영 데이터 또는 명시적 검수 결과가 있을 때만 적용한다.

### 판정 구간

| 점수 | 처리 |
|---:|---|
| 65~100 | Local 우선 채택 |
| 50~64 | 자동 채택 가능. `qualityScore >= 55`, confidence 0.70 이상, blocking flag 없음 필요 |
| 30~49 | 사람 검수. 상세 보강 또는 template 조정 후 채택 가능 |
| 0~29 | 기본 제외. 단, taxonomy 충돌·상세 결측만 원인이면 REVIEW |

최종 `selectionStatus` 규칙은 다음과 같다.

- `AUTO_ACCEPTED`: `localScore >= 50`, `qualityScore >= 55`, `classificationConfidence >= 0.70`, blocking review flag 없음
- `REVIEW`: `localScore 30~49`, 또는 `qualityScore 40~54`, 또는 taxonomy/template/safety 충돌 존재
- `EXCLUDED`: 명시적 하드 제외 유형이거나 `localScore < 30`이면서 taxonomy 충돌·데이터 보강 가능성도 없음
- `GENERIC_LOCAL_DISCOVERY` fallback은 점수와 무관하게 자동 게시하지 않고 `REVIEW`로 보낸다.

## 7. Data Quality Score

Local Score와 품질 점수를 섞지 않는다. 지역성이 높아도 사용자가 찾아갈 수 없는 데이터는 게시할 수 없기 때문이다.

overview/detail 결측은 이 점수에서만 반영한다. 결측 항목에는 `OVERVIEW_MISSING`, `DETAIL_NOT_ENRICHED`를 남기되 Local Score와 Quest type을 변경하지 않는다.

| 기준 | 배점 |
|---|---:|
| 유효한 좌표와 주소 | 25 |
| 영문 overview가 충분하고 행동 단서가 있음 | 20 |
| `lclsSystm3` 또는 `cat3` 존재 | 15 |
| 사용 가능한 대표 이미지와 저작권 코드 | 15 |
| 운영 시간·휴무·문의 등 상세 정보 | 10 |
| 최근 수정(`modifiedtime`) | 10 |
| 전화/웹 등 확인 수단 | 5 |

- 75 이상: 품질 우수
- 55~74: 자동 채택에 사용할 수 있는 최소 품질. 결측 필드는 비차단 flag로 기록
- 40~54: 검수 대기
- 39 이하: 적재만 하거나 제외

## 8. 지역·유형 균형 규칙

점수순 전체 상위만 뽑으면 서울·제주와 음식점에 편중될 수 있다. 최종 선택은 다음 quota를 권장한다.

- 지역 단위는 `lDongRegnCd + lDongSignguCd`를 우선하고, 없으면 `areacode + sigungucode`, 마지막으로 주소 파싱을 사용한다.
- 시군구마다 최소 seed 수(예: 5개)를 먼저 확보하고 그 뒤 점수순으로 증원한다.
- 한 시군구의 단일 Quest type은 최대 35%로 제한한다.
- 대표 관광지는 피드의 최대 20%, Local Score 70 이상 장소는 최소 50%가 되도록 구성한다.
- 같은 좌표 반경 50m, 유사 제목, 동일 전화번호를 중복 후보로 보고 대표 레코드 하나만 남긴다.
- 행사는 상설 콘텐츠 quota와 분리한다. 활성 기간에만 노출하고 종료 후 자동 archive한다.

## 9. 저장할 판정 결과

원본 필드를 보존하고 파생 판단을 별도 저장한다.

```json
{
  "source": "KTO_ENG_SERVICE_2",
  "contentId": "2947858",
  "sourceModifiedTime": "...",
  "normalizedRegion": { "regionCode": "26", "districtCode": "710" },
  "sourceTaxonomy": {
    "contentTypeId": "76",
    "cat": ["A01", "A0101", "A01010400"],
    "lcls": ["NA", "NA01", "NA010200"]
  },
  "localScore": 74,
  "qualityScore": 82,
  "selectionStatus": "AUTO_ACCEPTED",
  "scoreReasons": ["LOCAL_NATURE", "WALKABLE_ACTIVITY", "UNDERSERVED_DISTRICT"],
  "reviewFlags": []
}
```

점수와 함께 `scoreReasons`를 남겨 운영자가 왜 채택됐는지 설명할 수 있게 한다. 가중치나 사전을 바꿀 때는 `scoringVersion`을 저장해 재현성을 확보한다.

## 10. 운영 검증

- 초기 200건을 사람이 판정하여 규칙 결과와 비교한다.
- 자동 채택 precision을 우선하며 목표는 90% 이상으로 둔다.
- 지역별 채택률, 유형 분포, 결측률, 검수 반려 사유를 주간 집계한다.
- 사용자 신고, Quest 포기율, 위치 도착 후 인증 실패율을 점수 조정에 반영한다.
- API `modifiedtime` 변경 시 재평가하고, 삭제·장기 미갱신·행사 종료는 자동 비노출한다.
